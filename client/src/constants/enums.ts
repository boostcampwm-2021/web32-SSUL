export enum ToastMessageEnum {
  INFO = 'INFO',
  SUCCESS = 'SUCCESS',
  WARNING = 'WARNING',
  ERROR = 'ERROR',
}

export enum LeftOrRight {
  LEFT,
  RIGHT,
}

export enum MentorOrMentee {
  MENTOR = 'MENTOR',
  MENTEE = 'MENTEE',
}
