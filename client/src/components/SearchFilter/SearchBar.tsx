import React from 'react';
import styled from '@emotion/styled';

function SearchBar(): JSX.Element {
  return <Container>searchBar</Container>;
}

const Container = styled.div`
  display: flex;
`;

export default SearchBar;
