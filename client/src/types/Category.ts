export interface Category {
  id: number;
  name: string;
  imageUrl: string;
}

export interface CategoryData {
  id: number;
  name: string;
}

export interface CategoryResponse {
  id: number;
  name: string;
  imageUrl: string;
}
